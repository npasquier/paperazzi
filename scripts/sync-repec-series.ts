// Deprecated — Path B (offline RePEc→OpenAlex bridge) was removed in favour
// of RePEc-native search mode. Safe to `git rm`.
//
// See app/api/repec/search/route.ts + utils/repec/* for the live RePEc path.
export {};
